services:
  front:
    build:
      context: ./frontend
    volumes:
      - ./frontend/src:/frontapp/src
    ports:
      - "3030:3030"

  backend:
    build:
      context: ./backend
    volumes:
      - ./backend:/backapp
    ports:
      - "8080:8080"
